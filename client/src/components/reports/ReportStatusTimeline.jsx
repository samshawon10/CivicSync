const steps = [['pending', 'Submitted'], ['verified', 'Verified'], ['assigned', 'Assigned'], ['in_progress', 'In Progress'], ['completed', 'Completed'], ['closed', 'Closed']];
export default function ReportStatusTimeline({ status }) { const current = steps.findIndex(([value]) => value === status); return <ol className="space-y-0">{steps.map(([value, label], index) => { const done = index < current; const active = index === current; return <li key={value} className="flex gap-3"><div className="flex flex-col items-center"><span className={`grid h-6 w-6 place-items-center rounded-full border text-xs font-bold ${done ? 'border-civic-600 bg-civic-600 text-white' : active ? 'border-blue-600 bg-blue-600 text-white' : 'border-slate-300 bg-white text-slate-300'}`}>{done ? '✓' : active ? '●' : '○'}</span>{index < steps.length - 1 && <span className={`h-7 w-px ${done ? 'bg-civic-300' : 'bg-slate-200'}`} />}</div><div className="pb-4"><p className={`text-sm font-bold ${done || active ? 'text-ink' : 'text-slate-400'}`}>{label}</p>{active && <p className="text-xs text-slate-500">Current status</p>}</div></li>; })}</ol>; }

