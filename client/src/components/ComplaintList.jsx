const statusStyle = { Pending: 'bg-amber-100 text-amber-800', 'In Progress': 'bg-blue-100 text-blue-800', Resolved: 'bg-emerald-100 text-emerald-800' };
export default function ComplaintList({ complaints, loading, onEdit, onDelete }) {
  if (loading) return <div className="py-10 text-center text-slate-500">Loading complaints…</div>;
  if (!complaints.length) return <div className="rounded-xl border border-dashed border-slate-300 py-12 text-center text-slate-500">No complaints yet. Create one to get started.</div>;
  return <div className="overflow-x-auto"><table className="w-full min-w-185 text-left text-sm"><thead className="border-b border-slate-200 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-3 py-3">Issue</th><th className="px-3 py-3">Category</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Created</th><th className="px-3 py-3 text-right">Actions</th></tr></thead><tbody>{complaints.map((item) => <tr className="border-b border-slate-100 align-top" key={item._id}><td className="max-w-sm px-3 py-4"><p className="font-semibold text-ink">{item.title}</p><p className="mt-1 line-clamp-2 text-slate-500">{item.description}</p></td><td className="px-3 py-4 text-slate-600">{item.category}</td><td className="px-3 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${statusStyle[item.status]}`}>{item.status}</span></td><td className="px-3 py-4 text-slate-500">{new Date(item.createdAt).toLocaleDateString()}</td><td className="whitespace-nowrap px-3 py-4 text-right"><button onClick={() => onEdit(item)} className="mr-3 font-semibold text-civic-600 hover:underline">Edit</button><button onClick={() => onDelete(item)} className="font-semibold text-red-600 hover:underline">Delete</button></td></tr>)}</tbody></table></div>;
}

